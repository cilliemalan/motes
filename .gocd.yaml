pipelines:
  build:
    group: motes
    label_template: "${github[:8]}"
    locking: off
    materials:
      github:
        git: https://github.com/cilliemalan/motes.git
        branch: master
    stages:
      - prerequisites: # name of stage
          jobs:
            npm: # name of the job
              tasks:
                - script: echo hello world