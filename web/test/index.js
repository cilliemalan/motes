const assert = require('chai').assert;


describe('Testing system', () => {
    describe('internals', () => {
        it('should function', () => {
            assert.equal(2, 1 + 1);
        });
    });
});
